[project]
name = "vehictory-migratie"
version = "0.1.0"
description = "Eenmalige migratie van de oude Dash/parquet-data naar de nieuwe PostgreSQL-database."
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "pyarrow>=17.0",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
